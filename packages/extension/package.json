{
  "name": "@audio-tab-capture/extension",
  "version": "1.0.0",
  "description": "Chrome extension for audio tab capture",
  "scripts": {
    "build": "vite build && cp manifest.json dist/ && cp src/popup/popup.html src/popup/popup.css dist/",
    "dev": "vite build --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@audio-tab-capture/shared": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^4.4.0",
    "typescript": "^5.1.6",
    "@types/chrome": "^0.0.243"
  }
}