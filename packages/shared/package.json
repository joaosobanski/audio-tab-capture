{
  "name": "@audio-tab-capture/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for audio tab capture",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}