{
  "name": "@audio-tab-capture/web",
  "version": "1.0.0",
  "description": "Web interface for audio tab capture",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@audio-tab-capture/shared": "^1.0.0"
  },
  "devDependencies": {
    "vite": "^4.4.0",
    "typescript": "^5.1.6"
  }
}